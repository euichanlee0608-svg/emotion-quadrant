# CLAUDE.md — 감정의 사분면

전역 `~/.claude/CLAUDE.md`에 더해 이 폴더에만 적용되는 지침. 구조·엔진 설명은 `README.md`.

## 1. 데이터는 논문이 근거다

`data.js`는 **생성물이다. 직접 편집 금지** — `source/build_data.py`로 재생성한다.

- 평정값(v/a/p/f)은 박인조·민경환(2005) 부록 1에서 뽑은 것이다. **추정·보간·반올림 금지.**
  값이 이상해 보여도 논문 값이 맞다.
- `source/build_data.py`는 논문 표 4의 구간별 빈도를 체크섬으로 검증한다. 이 assert가 깨지면
  추출이 틀어진 것이니 **체크섬을 고치지 말고 추출을 고친다.**
- 뜻풀이(`d`)만 이 사이트가 붙인 것이다. 바꾸려면 `source/_defs_*.json`을 고치고 재생성한다.
- `r`(논문 `*` 대표단어 표시)은 부록에 86개뿐인데 본문은 87개라 해서 원문이 어긋나 있다.
  화면 구성에는 쓰지 말 것(결과에 표시만 한다).
- **의미 계층(`source/taxonomy.json`)은 논문에 없는 이 사이트의 분류다.** 논문은 225개를
  87범주로 묶었지만 구성원 목록을 싣지 않았다. 결과 화면·푸터에서 논문 값과 반드시 구분해 표기한다.

## 1.5 탐색은 좌표 줌이 아니라 **의미 트리**다 — 되돌리지 말 것

`engine.js`가 대분류 → 중분류 → 단어를 정확히 3단계로 걸어 내려간다. 좌표를 재귀적으로
좁히는 예전 방식으로 되돌리면 두 가지가 다시 깨진다(둘 다 실제로 겪었다).

1. **좌표 이웃 ≠ 의미 이웃.** `외롭다`와 `지루하다`는 좌표 거리 0.72로 붙어 있지만 딴 감정이다.
2. **모서리에서 막힌다.** `기쁘다`(v 5.94)보다 쾌-불쾌가 높은 단어는 434개 중 6개뿐이라
   사분면 하나가 통째로 비었다.

좌표는 버리는 게 아니라 **각 화면의 배치**로만 쓴다. `layout()`이 그 화면에 놓인 것들의
min~max로 다시 스케일하므로 사분면이 비지 않는다. 축은 레벨마다 다르다
(0·1단계 = 기분 × 세기, 2단계 = 세기 × 얼마나 쓰는 말인지).

`engine_test.mjs`의 **434개 전수 도달**과 **단어 화면 빈 사분면 평균**이 핵심 지표다.
탐색 로직을 바꾸면 이 두 값으로 판단할 것.

## 1.6 자연어 분석 — 기본은 Gemini, 답은 434개 안으로 가둔다

`analyze.js`: (로컬에서 열었을 때만 로컬 Ollama) → **Supabase Edge Function(Gemini flash-lite)** → `localMatch`.
- **공개 사이트에서 로컬 Ollama 를 쓰려고 하지 말 것.** https 페이지 → `http://127.0.0.1` 은
  크롬이 막는다(실측 `Failed to fetch`). `OLLAMA_ORIGINS` 를 열어도 소용없다.
  품질도 exaone3.5 < gemini-3.1-flash-lite 였다(README 비교표).
- `gemini_models` 는 **싼 순서**로 적어 둔다. 서버는 앞부터 시도하므로 평소엔 제일 싼 것만 쓴다.
- **모델 응답은 반드시 434개 안의 단어로 거른다**(서버·클라이언트 양쪽에서). 목록 밖의 말을
  통과시키면 결과 화면의 논문 평정값과 앞뒤가 안 맞는다.
- Edge Function 원본 사본은 `supabase/functions/emoquad-analyze/index.ts`. 배포본을 고치면
  이 사본도 같이 갱신한다. 키는 `app_secrets`에만 두고 프론트로 내보내지 않는다.
- 비로그인 공개 함수라 `emoquad_hit()` 일일 캡을 반드시 유지한다.
- 분석 결과는 텔레그램(COT봇)으로 알린다. 서버 처리분은 Edge Function 이, 로컬·폴백 처리분은
  클라이언트가 `{action:"notify"}` 로 중계한다(이 액션은 모델을 안 부르고 캡도 안 쓴다).
- **브라우저 검증에서 실제 분석 버튼을 누르지 말 것**(유료 경로). UI 존재만 확인하고,
  백엔드는 curl E2E 1건으로 따로 본다.

## 2. 표시 원칙

- 숫자는 **논문 평정값처럼 출처가 있는 것만** 보여 준다. "당신의 우울 지수 72%" 같은
  추정 점수를 만들어 내지 않는다(전역 지침 #11.6).
- 푸터의 출처 표기와 "심리 검사·진단이 아니다"라는 문구는 **지우지 않는다.**
- 뜻풀이가 논문 내용이 아니라는 고지도 유지한다.

## 3. 검증 게이트 (커밋 전 전부 통과)

```bash
node --check app.js
node --check engine.js && node --check analyze.js
node tests/engine_test.mjs      # 반드시 "✅ 전부 통과" (434개 전수 도달 포함)
python3 source/build_data.py    # data.js 재생성 후 git diff 가 비어야 정상
```

브라우저(엔진·CSS·레이아웃을 건드렸다면 필수):

```bash
python3 -m http.server 8777 &
/tmp/cdpvenv/bin/python tests/browser_check.py http://127.0.0.1:8777/index.html
```

**데스크톱 1280 + 모바일 390 두 뷰포트 모두** 통과해야 한다. 모바일만 깨지는 경우가
실제로 있었다(칩 겹침, 보드 이탈, 축 라벨이 본문과 겹침) — 데스크톱만 보고 넘어가지 말 것.

## 4. 레이아웃에서 조심할 것

- **칩 배치는 두 단계다**: `pickWords`가 정규화 좌표로 최소 간격을 두고 고르고,
  `relaxChips`가 그려진 뒤 실제 픽셀로 클램프·분리한다. 겹침 문제를 정규화 상수
  (`SEP_X`/`SEP_Y`)만 키워서 해결하려 하지 말 것 — 칩 폭은 글자 수와 화면 폭에 따라 변한다.
- `relaxChips`의 `GAP_Y`가 넉넉한 건 칩이 `bob` 애니메이션으로 최대 5px 흔들리기 때문이다.
  줄이면 특정 프레임에서 겹쳐 보인다.
- 축 라벨(`.axl`)은 `.board-outer`의 `--padX`/`--padY` 여백 안에 놓인다. 여백과 라벨 위치가
  같은 변수를 쓰므로 **한쪽만 바꾸면 겹친다.** 좁은 화면(≤860px)에서는 좌·하·우 라벨이
  보드 아래 한 줄로 내려간다.
  - ⚠️ 미디어쿼리에서 **`padding` 단축 속성을 통째로 덮어쓰지 말 것.** `--padY`는 그대로인데
    실제 padding만 바뀌어 라벨이 보드 위로 올라타 잘린다(실제로 겪음). 변수만 조정하고,
    아래 여백이 더 필요하면 `padding-bottom`만 따로 준다.
  - `.axl`의 `z-index:3`은 필수다. `.board`가 DOM 상 뒤에 와서 이게 없으면 라벨이 가려진다.
- 가로 스크롤은 어떤 화면에서도 생기면 안 된다(`browser_check.py`가 확인).
- **뜻 카드(`.sheet`)는 화면 하단 고정이다.** 문서 흐름에 두면 칩을 눌러도 "이 갈래로" 버튼이
  스크롤해야 보여서 처음 쓰는 사람이 못 찾는다(실제로 그랬다). `browser_check.py` 가
  ① 스크롤 없이 버튼이 뷰포트 안인지 ② 시트가 보드·축 라벨을 가리지 않는지 확인한다.
- 시트가 뜰 때 여백은 `.wrap` 에 준다. `.board-outer` 에 주면 그 안에 `bottom` 기준으로 놓인
  축 라벨이 같이 밀려 내려가 시트에 가려진다(실제로 그랬다).
- **지도 화면의 액션은 전부 시트 안에** 모은다(상단바에 흩어 두면 눌러야 할 곳을 못 찾는다).

## 5. 배포

정적 사이트라 백엔드·API 키가 없다. GitHub Pages(main/root) + `.nojekyll`.
방문 알림은 기존 GAS 비콘 공유(`ref` 접두사 `emomap|`), `file:`·localhost는 전송 스킵 —
헤드리스 검증이 실제 텔레그램을 울리지 않게 하는 조건이니 지우지 말 것.
푸터의 메인 포트폴리오 백링크도 유지한다(전역 지침 #11.7).
